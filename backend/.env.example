# Ajuste a porta se usar `POSTGRES_PORT` diferente no docker-compose.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/oficina_dois_dev?schema=public"
JWT_SECRET="defina-um-segredo-jwt-forte"
